import {
    StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    container: {
        width: '100%',
        // backgroundColor: 'red',
        paddingHorizontal: 10,
        marginVertical: 20,
    },
    persenData: {
        width: '45%',
        height: 180,
        backgroundColor: 'white',
        borderRadius: 10,
        borderColor: '#171717',
        shadowOpacity: 1,
        borderRadius: 10,
        elevation: 1,
        shadowRadius: 10,
    }
});

export default styles