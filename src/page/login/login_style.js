import {
    StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    container: {
        marginTop: 60,
        alignItems: 'center',
        height : '100%',
        width : '100%',
    },
    content: {
        width: '80%',
        height: '100%',
        
    },
});

export default styles